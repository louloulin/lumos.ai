基于 lumos4.2.md 中制定的 24 周改造计划，按照以下流程逐步实现功能：

## 执行流程

### 1. 选择任务
- 从 lumos4.2.md 的"阶段 1: 基础加固（Week 1-6）"开始
- 优先执行 P0（必须解决）级别的任务
- 建议从 Week 1 的测试基础设施开始

### 2. 实现功能
- 仔细阅读任务的具体要求和验收标准
- 使用 codebase-retrieval 查找相关代码位置
- 实现功能时遵循 CLAUDE.md 中的开发规范
- 确保代码符合 Rust 最佳实践（cargo fmt + cargo clippy）

### 3. 编写测试
- 为每个新功能编写单元测试
- 如果是跨模块功能，添加集成测试
- 确保测试覆盖边界情况和错误处理
- 运行测试确保通过：`cargo test --workspace`

### 4. 验证功能
- 运行相关测试套件验证功能正确性
- 使用 `cargo tarpaulin` 检查测试覆盖率是否提升
- 运行 `cargo clippy` 确保无警告
- 如果有性能要求，运行基准测试

### 5. 更新文档
- 在 lumos4.2.md 中标记已完成的任务（使用 ✅ 或 [x]）
- 更新 LUMOS4.2_TASK_TRACKER.md 中对应任务的状态
- 更新关键指标（如测试覆盖率、工具数量等）
- 记录实现过程中的重要决策和变更

## 具体要求

1. **每次只实现一个小任务**，不要一次性实现太多功能
2. **先运行 `./scripts/validate_lumos4.2.sh`** 获取当前基线数据
3. **优先级顺序**：P0 > P1 > P2
4. **建议起点**：Week 1 Day 1-2 的测试基础设施任务
5. **验证标准**：所有新代码必须有测试，测试必须通过
6. **文档更新**：每完成一个任务立即更新进度

## 输出要求

完成每个任务后，请提供：
- 实现的代码文件路径和关键改动
- 新增的测试数量和覆盖率变化
- 验证结果（测试通过情况）
- lumos4.2.md 和 LUMOS4.2_TASK_TRACKER.md 的更新内容
- 遇到的问题和解决方案

## 示例起点

建议从以下任务开始：
1. 运行 `./scripts/validate_lumos4.2.sh` 获取基线
2. 安装 `cargo-tarpaulin`：`cargo install cargo-tarpaulin`
3. 运行覆盖率测试：`cargo tarpaulin --workspace --out Html`
4. 为 Agent 模块添加 10-20 个单元测试
5. 验证测试通过并更新文档